"#[doc = \" Extension of `Governor` for simple, 3 options, vote counting.\"] pub\ntrait GovernorCounting\n{\n    #[doc =\n    \" Returns `true` if the account has voted for the proposal, `false` otherwise\"]\n    #[ink(message)] #[ink(selector = 4287511015u32)] fn\n    has_voted(& self, proposal_id : ProposalId, account : AccountId) -> bool ;\n    #[doc =\n    \" Returns the tuple (for, against, abstain) votes for a proposal, where `for` is the total\"]\n    #[doc =\n    \" number of votes for the proposal, `against` is the total number of votes against the\"]\n    #[doc =\n    \" proposal, and `abstain` is the total number of abstained votes.\"]\n    #[ink(message)] #[ink(selector = 3376251418u32)] fn\n    proposal_votes(& self, proposal_id : ProposalId) -> Result < ProposalVote,\n    GovernanceError > ;\n}"